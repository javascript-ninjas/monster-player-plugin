such UserSignOutController much $scope $timeout $state

    such redirect much
        plz $state.go with 'user-sign-in'
    wow

    plz $timeout with redirect, 1000
wow

module.exports is UserSignOutController
