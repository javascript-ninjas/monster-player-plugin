such UserPlaylistController much $scope $rootScope $http $state
    $scope.isAddFormDisplayed is false

    $scope.addPlaylist is such addPlaylist much name
        $scope.isAddFormDisplayed is false

        plz console.log with 'add playlist' name
    wow
wow

module.exports is UserPlaylistController
